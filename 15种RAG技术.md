1. 简单RAG: 简单的RAG 文本切块大小固定

2. 语义切分(Semantic Chunking):   按照意思来切，而不是字数来切

   计算相邻句子的相似度，如果相似度急剧下降，那么就进行切块，即切出的块很少，很难精准定位到正确答案，但却不怎么错。

   劣势: 召回率低

3. 上下文增强检索(Context Enriched Retrieval RAG): 检索到块时，自动带上它的相邻块，保留局部上下文，避免信息碎片化

4. 上下文分块标题(Contextual Chunk Headers): 在分块的时候给每一个块前面都加上一个章节的标题，然后再对标题做嵌入

5. 文档增强(Document Augmentation RAG): 反过来问问题，让大模型对每一个块来生成问题，也对这些问题进行向量化

6. 查询转换(Query Transformation): 当用户问题有歧义的时候，通过查询重写、回退提示、子查询分解这三种方式把用户的问题翻译的让大模型更好的理解

7. 重新排序器(Reranker): 初筛 -> 精筛

   初筛召回N个块-> 用LLM打分重排->选Top-K

   优势: 过滤初筛噪声

8. 关键段落提取(RSE): 检索到多个相关的块时，到原文中看看这些块是否连在一起

   **适用场景: ** 当相关块在原文中**连续出现**时，RSE能拼回完整上下文

9. 上下文压缩(Contextual Compression): 对于所检索到的块做一个压缩，只保留与问题相关的句子，删掉无关的内容(减少无关信息)。

10. 反馈循环(Feedback Loop): 通过持续学习实现性能迭代优化，越用越聪明。根据用户反馈修正对应的相关性评分。

    全程加入**反思**步骤。

    是否检索 -> 检索到块A -> 是否相关 -> 生成答案 ->  有依据吗？->  有用吗？

    结果: 保留该答案，若任一反思失败，则重试或放弃。

    **自我监督，可靠性高。**

11. 融合RAG(Fusion RAG): 融合两种检索方式，(语义向量搜索与基于关键词的BM25检索)

    兼顾语义和关键词，鲁棒性强

12. GraphRAG: 通过知识图谱来找关联，回答正确但是不够完整，则可以加入GraphRAG

    构建知识图谱，通过节点遍历找答案

    生成的答案直接基于图谱关系，精准可靠

    适合结构化知识、多跳推理

13. 层次RAG(Hierarchical Indices): 先给每一块或每一页做一个摘要的生成，当用户提问问题，先锁定对应的章节，然后找细节部分。

    在**长文档或文档集合**中大幅提升效率

14. 假设文档嵌入(HyDE, Hypothetical Document Embedding): 先猜答案，再找证据。

    提问 -> 生成假设文档 -> 创建嵌入假设文档 -> 检索文档 -> 生成响应 -> 评估响应 -> AI响应

    假设答案比原答案更"像"真实文档，语义更丰富，匹配更准。

15. 纠错型RAG(Corrective RAG): 对用户的查询评估相关性

    0.0 -> 0.3 : 使用网络检索

    0.3 -> 0.7 : 结合文档内容 + 网络内容做一个整合

    0.7 -> 1.0 : 直接使用


